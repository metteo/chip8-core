name: Chip8 Core Verify

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout sources
      uses: actions/checkout@v2
      
    - name: Build with Maven
      uses: "xlui/action-maven-cli/jdk11@master"
      with:
        args: -B clean verify --file pom.xml
